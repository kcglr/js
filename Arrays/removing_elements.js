const numbers = [1, 2, 3, 4];

//END
numbers.pop(); // removes the last element and returns it.

//BEGINNING
numbers.shift(); // removes the first element and returns it

//MIDDLE
numbers.splice(2, 1);
// 2-> index of the element that we want remove process to start
// 1-> amount of elements you want to remove
