// variables
let name = "kadir";
console.log(name);
// in order to see it on browser click inspect elements -> console

//Rules:
// they cannot be a reserved keyword such as if, var, let...
// they should be meaningful
// they cannot start with a number
// they cannot contain space or hypen (-)
// they are case-sensitive

let firstName = 'Kadir';
let lastName = 'Caglar';

// camel notation -> firstword letter starts with lowercase and secondword first letter is uppercase such as firstName
