// Constants
const interestRate = 0.3;
interestRate = 1;
console.log(interestRate);

// when we use const in front of variables we wont be able to change them.
// If you want to re-asign variable use "let" but if you dont need to re-assign use "const".
